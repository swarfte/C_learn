#include <stdio.h>
#include <math.h>

int main () {
    printf ("限定份數,方程等號為1時使用");
    int A , B , C,Exit;
    Exit = 1;
    while (Exit != 0) {
        printf("輸入X^2,Y^2所對應的數字");
        scanf ("%d%d",&A,&B);
    }
    printf ("test");
    return 0;
}
